{% extends '::base.html.twig' %}

{% block body -%}
    <h1>entreprise list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Secteur</th>
                <th>Forme</th>
                <th>Adresse</th>
                <th>Telephone</th>
                <th>Fax</th>
                <th>Mail</th>
                <th>Site</th>
                <th>Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('entreprise_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.secteur }}</td>
                <td>{{ entity.forme }}</td>
                <td>{{ entity.adresse }}</td>
                <td>{{ entity.telephone }}</td>
                <td>{{ entity.fax }}</td>
                <td>{{ entity.mail }}</td>
                <td>{{ entity.site }}</td>
                <td>{{ entity.type }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('entreprise_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('entreprise_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('entreprise_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
